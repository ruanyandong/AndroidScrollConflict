<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wings.conflict">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".example3.TenActivity" />
        <activity android:name=".example3.NineActivity" />
        <activity android:name=".example2.EightActivity" />
        <activity android:name=".example2.SevenActivity" />
        <activity android:name=".example2.SixActivity" />
        <activity android:name=".example1.FiveActivity" />
        <activity android:name=".example1.FourActivity" />
        <activity android:name=".example1.ThreeActivity" />
        <activity android:name=".example1.SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>